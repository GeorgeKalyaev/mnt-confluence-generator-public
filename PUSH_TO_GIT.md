# Инструкция по загрузке проекта в GitHub (приватный репозиторий)

## Шаг 1: Настройка Git (если еще не настроено)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

## Шаг 2: Создание приватного репозитория на GitHub

1. Откройте https://github.com/new
2. Введите название репозитория (например: `mnt-confluence-generator`)
3. **Выберите "Private"** (важно!)
4. НЕ создавайте README, .gitignore или license (они уже есть)
5. Нажмите "Create repository"

## Шаг 3: Добавление remote и push

После создания репозитория GitHub покажет команды. Выполните:

```bash
cd C:\Users\kalya\mnt-confluence-generator

# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/mnt-confluence-generator.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/mnt-confluence-generator.git

# Сделайте коммит (если еще не сделан)
git add .
git commit -m "Initial commit: МНТ Confluence Generator"

# Загрузите проект
git branch -M main
git push -u origin main
```

## Шаг 4: Проверка

Откройте репозиторий на GitHub и убедитесь, что:
- Репозиторий помечен как **Private** 🔒
- Все файлы загружены
- `.env` файл НЕ загружен (он в .gitignore)

## Важные замечания:

⚠️ **Безопасность:**
- Файл `.env` автоматически игнорируется (он в .gitignore)
- НЕ загружайте файл `.env` вручную - там могут быть пароли и токены
- При клонировании создайте новый `.env` на основе `.env.example` (если будет)

✅ **Что загружено:**
- Весь исходный код
- Все шаблоны
- Миграции БД
- Документация
- Скрипты запуска

❌ **Что НЕ загружено (благодаря .gitignore):**
- `.env` файлы
- Логи (`logs/`)
- Кэш Python (`__pycache__/`)
- Файлы IDE (`.vscode/`, `.idea/`)
