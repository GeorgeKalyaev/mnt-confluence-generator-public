# Подробная инструкция по установке

## Содержание

1. [Требования](#требования)
2. [Установка Python](#установка-python)
3. [Установка PostgreSQL](#установка-postgresql)
4. [Создание базы данных](#создание-базы-данных)
5. [Импорт схемы базы данных](#импорт-схемы-базы-данных)
6. [Установка зависимостей Python](#установка-зависимостей-python)
7. [Настройка конфигурации](#настройка-конфигурации)
8. [Запуск приложения](#запуск-приложения)
9. [Проверка работы](#проверка-работы)
10. [Решение проблем](#решение-проблем)

---

## Требования

- **Операционная система**: Windows 10/11, Linux, macOS
- **Python**: версия 3.10 или новее
- **PostgreSQL**: версия 12 или новее
- **Свободное место на диске**: минимум 500 МБ
- **Оперативная память**: минимум 2 ГБ

---

## Установка Python

### Windows

1. Скачайте Python 3.10+ с официального сайта: https://www.python.org/downloads/
2. Запустите установщик
3. **ВАЖНО**: Отметьте галочку **"Add Python to PATH"** при установке
4. Нажмите "Install Now"
5. После установки проверьте в командной строке (cmd или PowerShell):
   ```bash
   python --version
   ```
   Должно показать: `Python 3.10.x` или новее

### Linux

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install python3 python3-pip

# Проверка
python3 --version
```

### macOS

```bash
# Через Homebrew
brew install python3

# Проверка
python3 --version
```

---

## Установка PostgreSQL

### Windows

1. Скачайте PostgreSQL: https://www.postgresql.org/download/windows/
2. Запустите установщик
3. Следуйте инструкциям установщика
4. **Запомните пароль** для пользователя `postgres` - он понадобится позже
5. PostgreSQL обычно запускается автоматически как служба Windows

**Проверка установки:**
- Откройте pgAdmin из меню Пуск
- Или проверьте службу PostgreSQL в "Службы" Windows

### Linux

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install postgresql postgresql-contrib

# Запуск службы
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### macOS

```bash
# Через Homebrew
brew install postgresql
brew services start postgresql
```

---

## Создание базы данных

### Способ 1: Через pgAdmin (рекомендуется для Windows)

1. Откройте **pgAdmin** из меню Пуск
2. В левой панели найдите сервер PostgreSQL
3. Введите пароль пользователя `postgres` (если требуется)
4. Раскройте сервер → правый клик на "Databases"
5. Выберите "Create" → "Database"
6. В открывшемся окне:
   - **Name**: `mnt_db`
   - **Owner**: `postgres` (по умолчанию)
7. Нажмите "Save"

### Способ 2: Через командную строку

**Windows (cmd или PowerShell):**
```bash
psql -U postgres
```

Введите пароль пользователя `postgres`, затем:
```sql
CREATE DATABASE mnt_db;
\q
```

**Linux/macOS:**
```bash
sudo -u postgres psql
CREATE DATABASE mnt_db;
\q
```

---

## Импорт схемы базы данных

⚠️ **ВАЖНО**: Этот шаг обязателен! Без выполнения `schema.sql` приложение не сможет работать.

### Способ 1: Через pgAdmin (рекомендуется)

1. В pgAdmin выберите созданную базу данных `mnt_db`
2. Правый клик на `mnt_db` → "Query Tool"
3. В открывшемся окне нажмите "Open File" (или Ctrl+O)
4. Выберите файл `database/schema.sql` из проекта
5. Весь SQL скрипт отобразится в редакторе
6. Нажмите "Execute" (F5) или кнопку "Execute" на панели инструментов
7. В нижней панели должно появиться сообщение об успешном выполнении

**Что создается при выполнении schema.sql:**

✅ **Схема `mnt`** - изолированное пространство имен  
✅ **Таблица `mnt.documents`** - для хранения документов МНТ:
   - Все необходимые поля (id, title, project, author, data_json, status, и т.д.)
   - Поле `deleted_at` для мягкого удаления
   - Ограничения и проверки данных

✅ **Таблица `mnt.action_history`** - для истории действий пользователей

✅ **Индексы** для оптимизации производительности:
   - `idx_documents_status` - поиск по статусу
   - `idx_documents_project` - поиск по проекту
   - `idx_documents_author` - поиск по автору
   - `idx_documents_confluence_page_id` - поиск по Confluence page ID
   - `idx_documents_deleted_at` - фильтрация по мягкому удалению
   - `idx_documents_created_at` - сортировка по дате создания
   - `idx_documents_updated_at` - сортировка по дате обновления
   - `idx_documents_data_json_gin` - быстрый поиск по JSONB (теги, содержимое)
   - `idx_documents_status_deleted_created` - составной индекс для частых запросов
   - Индексы для таблицы `action_history`

✅ **Триггеры**:
   - Автоматическое обновление `updated_at` при изменении записи

✅ **Функции**:
   - `mnt.update_updated_at_column()` - функция для триггера

✅ **Комментарии** к таблицам и полям для документации

### Способ 2: Через командную строку

**Windows:**
```bash
cd путь\к\проекту\mnt-confluence-generator
psql -U postgres -d mnt_db -f database/schema.sql
```

Введите пароль пользователя `postgres` при запросе.

**Linux/macOS:**
```bash
cd путь/к/проекту/mnt-confluence-generator
sudo -u postgres psql -d mnt_db -f database/schema.sql
```

### Проверка успешного импорта

После выполнения скрипта проверьте в pgAdmin:

1. Раскройте `mnt_db` → "Schemas" → "mnt"
2. Должны быть видны:
   - **Tables**: `documents`, `action_history`
   - **Functions**: `update_updated_at_column`
   - **Triggers**: `update_documents_updated_at`

Или через командную строку:
```sql
psql -U postgres -d mnt_db
\dt mnt.*
\di mnt.*
\q
```

---

## Установка зависимостей Python

1. Откройте командную строку (cmd) или PowerShell
2. Перейдите в папку проекта:
   ```bash
   cd путь\к\проекту\mnt-confluence-generator
   ```
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

Если `pip` не найден, попробуйте:
```bash
python -m pip install -r requirements.txt
```

**Устанавливаемые пакеты:**
- `fastapi` - веб-фреймворк
- `uvicorn[standard]` - ASGI сервер
- `jinja2` - шаблонизатор
- `pydantic-settings` - настройки через переменные окружения (используется для конфигурации)
- `psycopg2-binary` - драйвер PostgreSQL
- `sqlalchemy` - ORM для работы с БД
- `pydantic` и `pydantic-settings` - валидация данных и настроек
- `httpx` - HTTP клиент для Confluence API
- `python-multipart` - обработка форм

---

## Настройка конфигурации

**📋 ВСЕ НАСТРОЙКИ В ОДНОМ МЕСТЕ - `app/core/config.py`**

### Настройка подключения к базе данных

1. Откройте файл `app/core/config.py`
2. Найдите секцию `Database Configuration` и измените:
   ```python
   database_password: str = "postgres"  # ⚠️ ИЗМЕНИТЕ НА СВОЙ ПАРОЛЬ!
   ```
   Замените значение на пароль пользователя PostgreSQL, который вы задали при установке.

### Настройка Confluence (опционально)

Если вы планируете публиковать документы в Confluence:

1. Откройте файл `app/core/config.py`
2. Найдите секцию `Confluence Configuration`

**Для Confluence Cloud:**
1. Получите API Token:
   - Откройте https://id.atlassian.com/manage-profile/security/api-tokens
   - Нажмите "Create API token"
   - Скопируйте токен
2. В `app/core/config.py` заполните:
   ```python
   confluence_url: str = "https://your-company.atlassian.net"
   confluence_email: Optional[str] = "your-email@example.com"
   confluence_api_token: Optional[str] = "your-api-token"
   ```

**Для Confluence Server/Datacenter:**
В `app/core/config.py` заполните:
```python
confluence_url: str = "http://localhost:8090"
confluence_username: str = "admin"
confluence_password: str = "admin"
```

**Примечание:** Если Confluence не настроен, приложение все равно будет работать, но публикация документов будет недоступна.

### Настройка логирования (опционально)

Все настройки логирования также находятся в `app/core/config.py` в секции `Logging Configuration`. По умолчанию используются оптимальные значения, но при необходимости их можно изменить.

**Альтернативный способ:** Можно также задать настройки через переменные окружения системы (они имеют приоритет над значениями в config.py):
```bash
# Windows (PowerShell)
$env:DATABASE_PASSWORD="your_password"
$env:LOG_LEVEL="INFO"

# Linux/macOS
export DATABASE_PASSWORD="your_password"
export LOG_LEVEL="INFO"
```

---

## Запуск приложения

1. Откройте командную строку (cmd) или PowerShell
2. Перейдите в папку проекта:
   ```bash
   cd путь\к\проекту\mnt-confluence-generator
   ```
3. Запустите приложение:
   ```bash
   python -m uvicorn app:app --reload --host 0.0.0.0 --port 8000
   ```

**Параметры запуска:**
- `--reload` - автоматическая перезагрузка при изменении кода (для разработки)
- `--host 0.0.0.0` - доступ с других устройств в сети (опционально)
- `--port 8000` - порт приложения (по умолчанию 8000)

После запуска вы увидите:
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [xxxxx]
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

---

## Проверка работы

1. **Откройте браузер** и перейдите по адресу:
   - http://localhost:8000
   - или http://127.0.0.1:8000

2. **Проверьте главную страницу:**
   - Должен открыться список МНТ (может быть пустым при первом запуске)

3. **Проверьте создание МНТ:**
   - Нажмите "Создать новый МНТ"
   - Заполните форму
   - Сохраните как черновик
   - Документ должен появиться в списке

4. **Проверьте подключение к БД:**
   - Если приложение запустилось без ошибок - подключение работает
   - Если есть ошибки подключения - проверьте параметры в `app/core/config.py`

---

## Решение проблем

### Проблема: "python не является внутренней или внешней командой"

**Причина:** Python не добавлен в PATH.

**Решение:**
1. Переустановите Python с галочкой "Add Python to PATH"
2. Или добавьте Python вручную в переменные окружения Windows:
   - Откройте "Переменные среды"
   - Добавьте путь к Python в переменную PATH

### Проблема: "ModuleNotFoundError: No module named 'fastapi'"

**Причина:** Зависимости не установлены.

**Решение:**
```bash
pip install -r requirements.txt
```

### Проблема: "could not connect to server" (PostgreSQL)

**Причина:** PostgreSQL не запущен или неправильные параметры подключения.

**Решение:**
1. Проверьте, что PostgreSQL запущен:
   - Windows: "Службы" → найдите "postgresql-x64-xx" → должна быть "Выполняется"
   - Linux: `sudo systemctl status postgresql`
   - macOS: `brew services list`
2. Проверьте параметры в `app/core/config.py`:
   - `DATABASE_HOST` - должен быть `localhost` для локальной установки
   - `DATABASE_PORT` - обычно `5432`
   - `DATABASE_NAME` - должно совпадать с созданной базой данных
   - `DATABASE_USER` - обычно `postgres`
   - `DATABASE_PASSWORD` - должен совпадать с паролем пользователя PostgreSQL

### Проблема: "relation mnt.documents does not exist"

**Причина:** Не выполнен скрипт `database/schema.sql`.

**Решение:**
1. Откройте pgAdmin
2. Выберите базу данных `mnt_db`
3. Откройте Query Tool
4. Выполните содержимое файла `database/schema.sql`
5. Перезапустите приложение

### Проблема: "password authentication failed"

**Причина:** Неправильный пароль в настройках.

**Решение:**
- Проверьте пароль пользователя PostgreSQL в файле `app/core/config.py` (параметр `database_password`)
- Пароль должен совпадать с паролем, заданным при установке PostgreSQL

### Проблема: "database mnt_db does not exist"

**Причина:** База данных не создана.

**Решение:**
1. Создайте базу данных через pgAdmin или командную строку:
   ```sql
   CREATE DATABASE mnt_db;
   ```
2. Затем выполните `database/schema.sql` в созданной базе данных

### Проблема: Приложение не запускается, ошибки в консоли

**Решение:**
1. Проверьте логи в консоли - там должна быть указана причина ошибки
2. Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
3. Проверьте, что Python версии 3.10+: `python --version`
4. Проверьте файл `app/core/config.py` - все обязательные параметры должны быть заполнены (особенно `database_password`)

---

## Следующие шаги

После успешной установки:

1. Прочитайте [QUICKSTART.md](QUICKSTART.md) для быстрого старта
2. Настройте Confluence (см. [QUICK_START_CONFLUENCE.md](QUICK_START_CONFLUENCE.md))
3. Изучите [LOGGING.md](LOGGING.md) для настройки логирования
4. Создайте первый МНТ через веб-интерфейс

---

**Готово!** Приложение установлено и готово к использованию. 🎉
